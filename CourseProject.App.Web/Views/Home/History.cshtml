@model IEnumerable<CourseProject.Core.DataAccess.Models.Song>
@{
    ViewData["Title"] = "History";
}

<div class="text-center">
    <table class="table">
        <thead>
        <tr>Uploads history</tr>
        </thead>
        <tbody>
        <tr>
            <td>Number</td>
            <td>Tittle</td>
            <td>Genre</td>
        </tr>    
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Tittle</td>
                <td>@item.Genre</td>
            </tr>
        }
        </tbody>
    </table>
</div>